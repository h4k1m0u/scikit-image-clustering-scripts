# Scikit-image Scripts:
Some basic `python` scipts using `scikit-image` libary.

## files:
**otsu_thresholding.py**: Perform a Otsu thresholding and show original and binarized image.

## Prerequisites:
**scikit-image**: `pip install scikit-image`
